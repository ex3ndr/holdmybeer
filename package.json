{
  "name": "holdmybeer",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace daycare run build",
    "dev": "yarn workspace daycare run dev",
    "bootstrap": "yarn workspace daycare run dev bootstrap",
    "rewrite": "yarn workspace daycare run dev rewrite",
    "test": "yarn workspaces run test",
    "test:watch": "yarn workspaces run test:watch",
    "typecheck": "yarn workspaces run typecheck"
  }
}
