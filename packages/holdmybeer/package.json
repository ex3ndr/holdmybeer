{
  "name": "holdmybeer",
  "version": "0.1.0",
  "description": "Holdmybeer CLI bootstrap and rewrite tool",
  "type": "module",
  "license": "MIT",
  "author": "ex3ndr",
  "bin": {
    "beer": "./bin/beer.js"
  },
  "files": [
    "dist",
    "bin",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cp sources/text/all.txt dist/text/",
    "dev": "tsx sources/main.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "fast-glob": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
