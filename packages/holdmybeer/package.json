{
  "name": "@slopus/beer",
  "version": "0.1.1",
  "description": "Holdmybeer CLI bootstrap tool",
  "type": "module",
  "license": "MIT",
  "author": "ex3ndr",
  "bin": {
    "beer": "./bin/beer.js"
  },
  "files": [
    "dist",
    "bin",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "text:gen": "bun sources/text/textGenBuild.ts",
    "build": "bun run text:gen && tsc -p tsconfig.json && cp sources/text/all.txt dist/text/",
    "dev": "bun run text:gen && bun sources/main.ts",
    "release": "bun run text:gen && bun sources/release/releaseRun.ts",
    "test": "bun run text:gen && vitest run",
    "test:watch": "bun run text:gen && vitest",
    "typecheck": "bun run text:gen && tsc -p tsconfig.typecheck.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sandbox-runtime": "0.0.37",
    "@inquirer/select": "^5.0.7",
    "commander": "^12.1.0",
    "fast-glob": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
